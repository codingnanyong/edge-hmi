# test/ environment (copy to .env)
# cp .env.example .env

REGISTRY=localhost
DB_IMAGE_TAG=latest
API_IMAGE_TAG=latest
# API_IMAGE_TAG_NEW=v1.0.0  # for defect_code_mst, defect_his, parts_mst

TZ=Asia/Seoul

POSTGRES_DB=edge_hmi
POSTGRES_USER=admin
POSTGRES_PASSWORD=
POSTGRES_SCHEMA=core
